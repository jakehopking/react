import React from 'react';
import styles from './Toolbar.scss';

const toolbar = (props) => {
  return (
    <header>
      <div>Menu</div>
      <div>Logo</div>
      <nav>...nav...</nav>
    </header>
  )
}

export default toolbar;
